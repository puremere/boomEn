<!DOCTYPE html>
<html lang="en-US">
<head>
    <title>

    </title>
    <link href="~/mydemo/question/css/main.css" rel="stylesheet" />
    <style>
        /*the container must be positioned relative:*/
        .custom-select {
            position: relative;
          
            width:80%;
            display:inline-block

        }

            .custom-select select {
                display: none; /*hide original SELECT element:*/
            }

        .select-selected {
            background-color: DodgerBlue;
        }

            /*style the arrow inside the select element:*/
            .select-selected:after {
                position: absolute;
                content: "";
                top: 14px;
                left: 10px;
                width: 0;
                height: 0;
                border: 6px solid transparent;
                border-color: #fff transparent transparent transparent;
            }

            /*point the arrow upwards when the select box is open (active):*/
            .select-selected.select-arrow-active:after {
                border-color: transparent transparent #fff transparent;
                top: 7px;
            }

        /*style the items (options), including the selected item:*/
        .select-items div, .select-selected {
            color: #ffffff;
            padding: 8px 16px;
            border: 1px solid transparent;
            border-color: transparent transparent rgba(0, 0, 0, 0.1) transparent;
            cursor: pointer;
            user-select: none;
        }

        /*style items (options):*/
        .select-items {
            position: absolute;
            background-color: DodgerBlue;
            top: 100%;
            left: 0;
            right: 0;
            z-index: 99;
        }

        /*hide the items when the select box is closed:*/
        .select-hide {
            display: none;
        }

        .select-items div:hover, .same-as-selected {
            background-color: rgba(0, 0, 0, 0.1);
        }
    </style>
    <script type="text/javascript" src="/Scripts/jquery-2.1.4.min.js"></script>
</head>
<body style="background-color:rgba(0, 0, 0, 0.1); padding:10px">
    <div id="contentholder" style="background-color:#fff;border-radius:10px">
        <div class="row" style="direction:rtl; width:80%; margin-right:auto; margin-left:auto; padding:25px; border-bottom:1px solid #eee">
            @*<p style=" display: inline-block"> انتخاب سوال :</p>*@
            <div class="custom-select" style="" >
                <select id="mydropdown">
                 
                    <option class="op" value="1" selected>آیا دوچرخه سواری بلد هستید؟ </option>
                    <option class="op" value="2">آیا دوچرخه سواری انجام میدهید؟</option>
                    <option class="op" value="3">آیا دوچرخه دارید؟ </option>
                    <option value="4">از طرح دوچرخه شهری خبر دارید؟</option>
                    <option value="5">آیا اگر دوچرخه به راحتی در دسترس شما قرار گیرد حاضر به استفاده از آن هستید؟</option>
                    <option value="6">اگر خیر چرا؟</option>
                    <option value="7">اگر مسیر دوچرخه وجود داشته باشد حاضر به استفاده از دوچرخه هستید؟</option>
                    <option value="8">در فصل زمستان در صورت عدم بارندگی حاضر به استفاده از دوچرخه هستید؟</option>
                    <option value="9">در هوای آلوده حاضر به استفاده از دوچرخه هستید؟</option>
                    <option value="10">اگر از نظر علمی بدانید دوچرخه سواری در هوای آلوده مفید است آیا حاضر به استفاده از دوچرخه هستید؟</option>
                    <option value="11">برای نیم ساعت استفاده از دوچرخه دنده ای حاضرید 1000 @Resources.res.toman بپردازید؟</option>
                    <option value="12">برای ثبت نام در طرح دوچرخه شهری حاضرید 20000 هزار @Resources.res.toman بپردازید؟</option>
                   <option value="13">با وجود سربالایی حاضر به استفاده از دوچرخه هستید؟</option>
                    <option value="14">در مسیر های کوتاه با سراشیبی اسکوتر را ترجیح میدهید؟</option>
                    <option value="15">به نظر شما کدام یک از موارد زیر میتواند انگیزه بیشتری جهت استفاده از دوچرخه باشد؟</option>
                   
                </select>
            </div>
            <script>
                $(document).ready(function () {
                  
                    an
                   
                  
                })
              
            </script>
            <script>
                var x, i, j, selElmnt, a, b, c;
                /*look for any elements with the class "custom-select":*/
                x = document.getElementsByClassName("custom-select");
                for (i = 0; i <x.length; i++) {
                    selElmnt = x[i].getElementsByTagName("select")[0];
                    /*for each element, create a new DIV that will act as the selected item:*/
                    a = document.createElement("DIV");
                    a.setAttribute("class", "select-selected");
                    a.innerHTML = selElmnt.options[selElmnt.selectedIndex].innerHTML;
                    x[i].appendChild(a);
                    /*for each element, create a new DIV that will contain the option list:*/
                    b = document.createElement("DIV");
                    b.setAttribute("class", "select-items select-hide");
                    for (j = 1; j <selElmnt.length; j++) {
                        /*for each option in the original select element,
                        create a new DIV that will act as an option item:*/
                        c = document.createElement("DIV");
                        c.innerHTML = selElmnt.options[j].innerHTML;
                        c.addEventListener("click", function (e) {
                            /*when an item is clicked, update the original select box,
                            and the selected item:*/
                            var y, i, k, s, h;
                            s = this.parentNode.parentNode.getElementsByTagName("select")[0];
                            h = this.parentNode.previousSibling;
                            for (i = 0; i <s.length; i++) {
                                if (s.options[i].innerHTML == this.innerHTML) {
                                    s.selectedIndex = i;
                                    h.innerHTML = this.innerHTML;
                                    y = this.parentNode.getElementsByClassName("same-as-selected");
                                    for (k = 0; k <y.length; k++) {
                                        y[k].removeAttribute("class");
                                    }
                                    this.setAttribute("class", "same-as-selected");
                                    break;
                                }
                            }
                            h.click();
                        });
                        b.appendChild(c);
                    }
                    x[i].appendChild(b);
                    a.addEventListener("click", function (e) {
                     
                        var classname = $(this).attr('class');
                        if (classname !== "select-selected") {
                            var value = $(this).text();
                            $("option").each(function () {
                                var itsval = $(this).text();
                                
                                if (itsval === value) {
                                    var id = $(this).val();
                                    
                                    $.ajax({
                                        url: '/Demo/getchart',
                                        dataType: 'html',
                                        data: {
                                            val : id
                                        },
                                        success: function (resp) {
                                            
                                            $('#paiholder').html(resp);
                                        },
                                        error: function (req, status, err) {
                                          alert('something went wrong', status, err);
                                        }
                                    });
                                }
                            });
                        }
                        

                        /*when the select box is clicked, close any other select boxes,
                        and open/close the current select box:*/
                        e.stopPropagation();
                        closeAllSelect(this);
                        this.nextSibling.classList.toggle("select-hide");
                        this.classList.toggle("select-arrow-active");
                    });
                }
                function closeAllSelect(elmnt) {
                    /*a function that will close all select boxes in the document,
                    except the current select box:*/
                    var x, y, i, arrNo = [];
                    x = document.getElementsByClassName("select-items");
                    y = document.getElementsByClassName("select-selected");
                    for (i = 0; i <y.length; i++) {
                        if (elmnt == y[i]) {
                            arrNo.push(i)
                        } else {
                            y[i].classList.remove("select-arrow-active");
                        }
                    }
                    for (i = 0; i <x.length; i++) {
                        if (arrNo.indexOf(i)) {
                            x[i].classList.add("select-hide");
                        }
                    }
                }
                /*if the user clicks anywhere outside the select box,
                then close all select boxes:*/
                document.addEventListener("click", closeAllSelect);
            </script>

        </div>
       <div id="paiholder">
           <div id="piechart" style="display:inline-block"></div>
           <div id="sexpiechart" style="display: inline-block"></div>
           <div id="resultpiechart" style="display: inline-block"></div>
           <div id="piechartone" style="display: inline-block"></div>
           <div id="piechart2" style="display: inline-block"></div>
           <div id="piechart3" style="display: inline-block"></div>
           <div id="piechart4" style="display: inline-block"></div>
           <div id="piechart5" style="display: inline-block"></div>
           <div id="piechart6" style="display: inline-block"></div>

           <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>

           <script type="text/javascript">
               // Load google charts
               google.charts.load('current', { 'packages': ['corechart'] });
               google.charts.setOnLoadCallback(drawChart);
               google.charts.setOnLoadCallback(drawChartsex);
               google.charts.setOnLoadCallback(drawChartresult);
               google.charts.setOnLoadCallback(drawChartone);
               google.charts.setOnLoadCallback(drawChart2);
               google.charts.setOnLoadCallback(drawChart3);
               google.charts.setOnLoadCallback(drawChart4);
               google.charts.setOnLoadCallback(drawChart5);
               google.charts.setOnLoadCallback(drawChart6);

               // Draw the chart and set the chart values
               function drawChart() {
                   var data = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['مرد زیر بیست سال', 368],
                   [' مرد بین 20 تا 40 سال', 2056],
                   ['مرد بالای 40 سال', 644],
                   ['زن زیر بیست سال', 53],
                   ['زن بین 20 تا 40 سال', 427],
                   ['زن بین بالای 40 سال', 47]

                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options = { 'title': 'پراکندگی سن بر اساس جنس -3653 نفر', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var chart = new google.visualization.PieChart(document.getElementById('piechart'));
                   chart.draw(data, options);
               }
               function drawChartsex() {
                   var sexdata = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['مرد', 3069],
                   ['زن', 584],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var sexoptions = { 'title': 'پراکندگی جنسیتی', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('sexpiechart'));
                   sexchart.draw(sexdata, sexoptions);
               }
               function drawChartresult() {
                   var sexdata = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 3162],
                   ['خیر', 431],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var sexoptions = { 'title': 'پاسخ نهایی', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('resultpiechart'));
                   sexchart.draw(sexdata, sexoptions);
               }
               function drawChartone() {
                   var dataone = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 339],
                   ['خیر', 29],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var optionsone = { 'title': 'مرد زیر بیست سال', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechartone'));
                   sexchart.draw(dataone, optionsone);
               }
               function drawChart2() {
                   var data2 = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 1830],
                   ['خیر', 226],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options2 = { 'title': 'مرد بین 20 تا 40', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechart2'));
                   sexchart.draw(data2, options2);
               }
               function drawChart3() {
                   var data2 = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 554],
                   ['خیر', 90],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options2 = { 'title': 'مرد بالای 40', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechart3'));
                   sexchart.draw(data2, options2);
               }
               function drawChart4() {
                   var data2 = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 45],
                   ['خیر', 7],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options2 = { 'title': 'زن زیر بیست سال', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechart4'));
                   sexchart.draw(data2, options2);
               }
               function drawChart5() {
                   var data2 = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 358],
                   ['خیر', 68],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options2 = { 'title': 'زن بین 20 تا 30 ', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechart5'));
                   sexchart.draw(data2, options2);
               }
               function drawChart6() {
                   var data2 = google.visualization.arrayToDataTable([
                   ['Task', 'Hours per Day'],
                   ['بله', 36],
                   ['خیر', 11],


                   ]);

                   // Optional; add a title and set the width and height of the chart
                   var options2 = { 'title': 'زن بالای 40', 'width': 400, 'height': 300 };

                   // Display the chart inside the <div> element with id="piechart"
                   var sexchart = new google.visualization.PieChart(document.getElementById('piechart6'));
                   sexchart.draw(data2, options2);
               }
           </script>
       </div>
       

        @*<div id="sexpiechart" style=""></div>
        <script type="text/javascript">
            // Load google charts
            google.charts.load('current', { 'packages': ['corechart'] });
            google.charts.setOnLoadCallback(drawChartsex);

            // Draw the chart and set the chart values
            function drawChartsex() {
                var sexdata = google.visualization.arrayToDataTable([
                ['Task', 'Hours per Day'],
                ['مرد', 3069],
                ['زن', 584],
                

                ]);

                // Optional; add a title and set the width and height of the chart
                var sexoptions = { 'title': 'پراکندگی جنسیتی', 'width': 400, 'height': 300 };

                // Display the chart inside the <div> element with id="piechart"
                var sexchart = new google.visualization.PieChart(document.getElementById('sexpiechart'));
                sexchart.draw(sexdata, sexoptions);
            }
        </script>*@




        @*<div id="piechartone" style="display:inline-block"></div>
        <script type="text/javascript">
            // Load google charts
            google.charts.load('current', { 'packages': ['corechart'] });
            google.charts.setOnLoadCallback(drawChartone);

            // Draw the chart and set the chart values
            function drawChartone() {
                var dataone = google.visualization.arrayToDataTable([
                ['Task', 'Hours per Day'],
                ['بله', 339],
                ['خیر', 29],


                ]);

                // Optional; add a title and set the width and height of the chart
                var optionsone = { 'title': 'مرد زیر بیس سال', 'width': 400, 'height': 300 };

                // Display the chart inside the <div> element with id="piechart"
                var chart1 = new google.visualization.PieChart(document.getElementById('piechartone'));
                sexchart.draw(dataone, optionsone);
            }
        </script>*@

    </div>
   

</body>
</html>
